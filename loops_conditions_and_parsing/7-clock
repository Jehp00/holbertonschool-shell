#!/usr/bin/env bash
#displays the time for 12 hours and 59 minutes.
i=1
j=1
while [ $i -lt 59 ]
do
	if [ $i -eq 60 ]
	then
		echo $i
		i=$((i+1))
	elif [ $j -lt 12 ]
	then
		echo 'Hola:' $j
		j=$((i+1))
		return $i
	fi
done
		
