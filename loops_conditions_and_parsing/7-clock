#!/usr/bin/env bash
#displays the time for 12 hours and 59 minutes.
i=1
j=1
while [ $i -le 60 ][ $j -le 12 ]
do
	if [ $i -gt 59 ]
	then
		echo 'Hora:' $j
		j=$((j+1))
	else
		echo $i
		i=$((i+1))
		echo 'Hora' $j
	fi
done
		
